# PostgreSQL Configuration for Experiment Tracking

DB_HOST=localhost
DB_PORT=5432
DB_NAME=ml_experiments
DB_USER=postgres
DB_PASSWORD=your_password_here

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your PostgreSQL credentials
# 3. Database setup is optional - the script works without it